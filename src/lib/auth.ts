// TODO (Fase 3): Auth.js v5 config with Credentials provider.
// - Validate email + password against User where role === ADMIN.
// - Use bcrypt to compare hashed passwords.

export const authPlaceholder = true;
